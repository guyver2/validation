/* Write your global styles here, in PostCSS syntax */
@tailwind base;
@tailwind components;
@tailwind utilities;

html,
body {
	@apply h-full overflow-hidden;
}

:root {
	--theme-rounded-container: theme(borderRadius.DEFAULT);
	--theme-rounded-base: theme(borderRadius.DEFAULT);
}
